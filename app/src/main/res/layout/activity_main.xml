package com.meupet.virtual;

import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Intent;
import android.graphics.Color;
import android.os.Bundle;
import android.os.Handler;
import android.widget.Button;
import android.widget.ImageView;
import android.widget.ProgressBar;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.app.NotificationCompat;

public class MainActivity extends AppCompatActivity {

    private int fome = 100;
    private ProgressBar barraFome;
    private Handler handler = new Handler();
    private ImageView pet;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        // Inicializa os componentes da tela
        barraFome = findViewById(R.id.barraFome);
        pet = findViewById(R.id.petImageView);
        Button btnAlimentar = findViewById(R.id.btnAlimentar);

        // Permissão para Android 15
        if (android.os.Build.VERSION.SDK_INT >= 33) {
            requestPermissions(new String[]{"android.permission.POST_NOTIFICATIONS"}, 101);
        }

        criarCanalNotificacao();
        iniciarCicloVida();

        btnAlimentar.setOnClickListener(v -> {
            fome = 100;
            barraFome.setProgress(fome);
            pet.clearColorFilter();
        });
    }

    private void iniciarCicloVida() {
        handler.postDelayed(new Runnable() {
            @Override
            public void run() {
                if (fome > 0) {
                    fome -= 10;
                    barraFome.setProgress(fome);
                    
                    if (fome == 20) enviarNotificacaoFome();
                    if (fome <= 0) pet.setColorFilter(Color.GRAY);
                }
                handler.postDelayed(this, 5000); // 5 segundos
            }
        }, 5000);
    }

    private void criarCanalNotificacao() {
        if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.O) {
            NotificationChannel channel = new NotificationChannel("PET_ALERTA", "Avisos do Pet", NotificationManager.IMPORTANCE_HIGH);
            getSystemService(NotificationManager.class).createNotificationChannel(channel);
        }
    }

    private void enviarNotificacaoFome() {
        Intent intent = new Intent(this, MainActivity.class);
        PendingIntent pIntent = PendingIntent.getActivity(this, 0, intent, PendingIntent.FLAG_IMMUTABLE);

        NotificationCompat.Builder builder = new NotificationCompat.Builder(this, "PET_ALERTA")
                .setSmallIcon(android.R.drawable.ic_dialog_info)
                .setContentTitle("Seu Pet está com fome!")
                .setContentText("Me dê comida, por favor!")
                .setContentIntent(pIntent)
                .setAutoCancel(true);

        ((NotificationManager) getSystemService(NOTIFICATION_SERVICE)).notify(1, builder.build());
    }
}
